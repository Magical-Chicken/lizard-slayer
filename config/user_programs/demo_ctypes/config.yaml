info:
    name: Demo CTypes
    version: 0.1
    # if true, build c extention, if false, use ctypes, default is true
    # NOTE: ctypes is currently only supported user program type
    py_c_extention: false
sources:
    python: python_funcs.py
    header: program.h
    cuda: kernel.cu
code: {}
